// Non primitives
const person = {
	name: "Chitraksh",
	age: 19,
	isStudent: true,
};

// Console logging the object
console.log(person);
// Console logging a specific element out of the object
console.log(person.name);
